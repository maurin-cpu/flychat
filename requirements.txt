requests>=2.31.0
openai>=1.10.0
# Optional LLM-Provider (werden erst geladen, wenn CHAT_PROVIDER / ANALYSIS_PROVIDER gesetzt).
# Siehe config.py + llm_client.py. Bei Bedarf einkommentieren.
anthropic>=0.40.0
google-genai>=1.0.0
python-dotenv>=1.0.0
flask>=3.0.0
shapely>=2.0.0
Pillow>=10.0.0
# Frontenextraktion aus den DWD-Karten (scripts/archive_dwd_fronten.py und die
# experiment_fronten_*-Skripte): Bildarithmetik und Skelettierung.
numpy>=1.26.0
scipy>=1.11.0
